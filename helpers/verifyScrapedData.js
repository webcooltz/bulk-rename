// very scraped data:
// -verify the metadata is complete
// -check for empty properties and objects

// If it scraped correctly, move forward with changing metadata

// Pending predecessor:
// -Adding scrape to the main function